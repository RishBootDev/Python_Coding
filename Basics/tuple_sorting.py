# I am given a tuple and asked to sort this tuple

tup = (45, 23, 67, 1, 99, 5)

list = sorted(tup)
print(list)

# the tuples are immutable and we cannot modify the data inside them 
# so we use the sorted method that returns the sorted list form